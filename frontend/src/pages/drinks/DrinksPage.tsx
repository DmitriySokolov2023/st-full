import {useTitle} from "../../hooks/useTitle.ts";

const DrinksPage = () => {
    useTitle('Напитки')
    return (
        <div>
            <h1>Напитки</h1>
        </div>
    );
};

export default DrinksPage;