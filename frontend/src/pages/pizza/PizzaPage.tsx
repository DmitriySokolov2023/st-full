import {useTitle} from "../../hooks/useTitle.ts";



const PizzaPage = () => {
    useTitle('Пицца')

    return (
        <div>
            <h1>Пицца</h1>
        </div>
    );
};

export default PizzaPage;