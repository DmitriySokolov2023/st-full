import {useTitle} from "../../hooks/useTitle.ts";

const SnacksPage = () => {
    useTitle('Закуски')
    return (
        <div>
            <h1>Закуски</h1>
        </div>
    );
};

export default SnacksPage;