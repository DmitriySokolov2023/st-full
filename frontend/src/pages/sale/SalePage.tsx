import {useTitle} from "../../hooks/useTitle.ts";

const SalePage = () => {
    useTitle('Акции')
    return (
        <div>
            <h1>Акции</h1>
        </div>
    );
};

export default SalePage;