import {useTitle} from "../../hooks/useTitle.ts";

const DessertsPage = () => {
    useTitle('Десерты')
    return (
        <div>
            <h1>Десерты</h1>
        </div>
    );
};

export default DessertsPage;